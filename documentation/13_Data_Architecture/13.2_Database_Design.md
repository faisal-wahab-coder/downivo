# 13.2 Database Design

Single file DB via `sqflite` `openDatabase` path from `resolveDatabasePath()`. Version 4 with `onCreate` + `onUpgrade` branches `<2`, `<3`, `<4`. Tests: DB-001 create, DB-005 upgrade keeps rows.
