# 13.1 Data Architecture Overview

Three stores:

1. **sqflite** — downloads + app_metadata (schema v4)
2. **shared_preferences** — settings, onboarding, recent searches, favorites ids, browser session
3. **Filesystem** — actual files under StoragePaths categories

No remote API database. No Drift. No WatermelonDB. No Hive.
