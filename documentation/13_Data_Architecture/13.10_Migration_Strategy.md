# 13.10 Migration Strategy

`onUpgrade` in `AppDatabase`:

- old < 2: created_at + updated_at indexes
- old < 3: file_path index
- old < 4: ALTER ADD thumbnail_url, platform, title

Always `IF NOT EXISTS` for indexes. Never drop user rows.
