# 13.3 Entity Models

| Model | Package | Role |
|-------|---------|------|
| `DownloadRecord` | database | SQLite row |
| `DownloadTask` | download_engine | runtime + UI |
| `DiscoveredResource` | download_engine | resolver output |
| `LibraryFile` | media_library | scanned file |
| `CategorySummary` | media_library/storage | folder stats |
| Settings state | app_core settingsProvider | prefs |

Map Task ↔ Record in `DownloadRepository`. After rename/move, `updateDownloadFilePath`.
