# 11.12 CI/CD

As-built:

- GitHub Actions: analyze + test + AAB on `main`
- Release workflow on version tags
- Local: `bash scripts/ci.sh` from `universal_downloader/`
- Melos scripts: analyze, test, qa (`qa/runner/qa_runner.sh`), format
- Signing: `key.properties.example` — never commit real keystores
- Version: `1.2.0+4` in `apps/mobile/pubspec.yaml`

See original `universal_downloader/RELEASE.md` and `CHANGELOG.md` for release engineering copy.
