# ADR-006: Android-Only V1

**Status:** Accepted for product focus

V1 **product** is Android 10+ (API 29).

As-built also includes `apps/web` as a second Flutter runner with stubs. Recreate web if you want an exact copy; Android is mandatory.
