# 10.8 Smart Search

**Not a tab.** Pushed `AppSearchScreen` from Home/Downloads.

**Package:** `search` (pub name `udm_search`).

Queries files + download history. Recent chips persist (`recent_searches` prefs). Empty / no-results / clear.

Files still has its own in-list `LibraryQuery.search`. Do not add `/search` to the shell.
