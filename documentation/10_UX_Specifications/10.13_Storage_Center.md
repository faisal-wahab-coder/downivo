# 10.13 Storage Center

**Not a route.** Recreate as `StorageDashboard` on Home and Settings.

Shows managed used bytes (`CategorySummary`) and, on Android, volume available/total (`StorageInfo` / StatFs). Hidden when `hasVolumeStats` is false (web).

Tap → Files. Download folder path is **read-only** in Settings.
