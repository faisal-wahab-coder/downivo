# 12.2 Design Principles

- Package boundaries over giant `lib/` in the app
- UI in app_core; engines in packages
- Prefer extending `ContentProviderRegistry` over modifying UI for a new host
- Fail closed on private/auth content
- Match existing widget structure rather than inventing new shells
