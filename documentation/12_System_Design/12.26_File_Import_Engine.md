# 12.26 File Import Engine

Share intake (`IntakeActionType.importFiles`) copies into unmanaged/temp then Files `_ImportSheet` moves into a `StorageCategory`. `pendingImportsProvider`. FR-035 style detection of newly added files.
