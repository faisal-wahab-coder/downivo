# 12.6 Repository Pattern

`DownloadRepository` in `download_engine` is the download metadata repository (wraps `AppDatabase`).

Library files are **not** a second SQLite table — they are filesystem scans (`media_library`) plus favorites in SharedPreferences.

Settings are SharedPreferences via `settingsProvider`.
