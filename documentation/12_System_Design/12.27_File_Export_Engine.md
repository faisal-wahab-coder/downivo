# 12.27 File Export Engine

**DO NOT BUILD a dedicated export UI.** Share of a single file via `share_plus` is enough (Files / completed tile). Photos and videos also have **Save to Gallery**, which copies into MediaStore and leaves the managed original in place.
