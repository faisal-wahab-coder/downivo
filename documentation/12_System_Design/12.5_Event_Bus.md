# 12.5 Event Bus

**DO NOT implement a standalone event bus package.**

`DownloadManager.tasksStream` (broadcast) plus Riverpod `ref.watch` is the only cross-feature realtime channel required for V1.
