# 12.14 Media Library

`media_library`: `LibraryFile`, browse pages, scan, favorites store, open/share/rename/move/delete, save photos/videos to the system gallery. 30s TTL scan cache via `performance`. Folder counts from cache.
