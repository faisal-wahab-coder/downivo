# 12.4 Dependency Injection

**As-built: Riverpod `ProviderContainer` overrides in `bootstrap()`.**

Do not introduce GetIt/injectable. Construct `DownloadManager` in `AppInitializer.createDownloadManager`. Pass `StoragePaths` and repository. Feature packages receive collaborators via constructors where tested.
