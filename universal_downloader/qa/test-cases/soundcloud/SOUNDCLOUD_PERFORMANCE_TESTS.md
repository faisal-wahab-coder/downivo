# SoundCloud Performance Tests

## Test File
`packages/download_engine/test/soundcloud_performance_test.dart`

25-track playlist returns URL strings only (no audio bytes in memory). Long-path classify stays cheap.
