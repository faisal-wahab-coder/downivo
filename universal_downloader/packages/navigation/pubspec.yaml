name: navigation
description: Application navigation with go_router.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.2.0
  design_system:
    path: ../design_system
  permissions:
    path: ../permissions
  shared_types:
    path: ../shared_types

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
